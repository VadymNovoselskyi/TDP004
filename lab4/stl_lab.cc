#include <iostream>
#include <string>
#include <vector>

using namespace std;

struct argument {
    string flag;
    string parameter;
};

vector<argument> v = { {"--print", ""},
                       {"--frequency", ""},
                       {"--table", ""},
                       {"--substitute", ""},
                       {"--remove", ""} };


int main(int argc, char *argv[]) {

    // switch (argument) {
    //     case --print:

    //     case --frequency:

    //     case --table:

    //     case --substitute:

    //     case --remove:

    //     case invaid_argument:
    // }

}